Food Delivery Service
Мини-MVP сервиса доставки еды с полным циклом от пользователя до оплаты заказа.

🚀 Технологии
Java 17

Spring Boot 3

Spring Data JPA/Hibernate

H2 Database (in-memory)

Spring Validation

MapStruct для маппинга DTO

SpringDoc OpenAPI (Swagger UI)

Maven

📋 Функциональность
Основные сущности
Пользователи - регистрация, профили, роли (CUSTOMER, RESTAURANT, COURIER, ADMIN)

Рестораны - создание, меню, фильтрация по кухне и рейтингу

Меню - блюда с вариациями (размеры, цены, время готовки)

Корзина - добавление/удаление позиций, подсчет суммы и ETA

Заказы - создание из корзины, статусы, отмена

Платежи - интеграция с внешними системами, идемпотентность

Бизнес-правила
✅ Минимальная сумма заказа: 300.00 ₽

✅ Один ресторан в корзине

✅ Проверка доступности вариаций при создании заказа

✅ Запрет добавления в закрытый ресторан

✅ Валидные переходы статусов заказа

✅ Идемпотентность платежей по externalId

🏃 Быстрый старт
Запуск приложения
bash
mvn spring-boot:run
Полезные ссылки после запуска
Swagger UI: http://localhost:8080/swagger-ui/index.html

H2 Console: http://localhost:8080/h2-console

JDBC URL: jdbc:h2:mem:fooddb

User: sa

Password: (пусто)

📊 Основные эндпоинты
Пользователи
POST /users - создать пользователя

GET /users/{id} - получить профиль

PUT /users/{id} - обновить профиль

GET /users/{id}/versions/at - история изменений

Рестораны и меню
POST /restaurants - создать ресторан

GET /restaurants - список с фильтрами

POST /restaurants/{id}/menu - добавить блюдо

PATCH /menu/{id}/availability - изменить доступность вариации

Корзина
GET /cart?userId={id} - получить корзину

POST /cart/items - добавить позицию

DELETE /cart?userId={id} - очистить корзину

Заказы
POST /orders - создать заказ из корзины

GET /orders/{id} - получить заказ

PATCH /orders/{id}/status - изменить статус

DELETE /orders/{id} - отменить заказ

Платежи
POST /payments - создать платёж

PATCH /payments/{id}/status - обновить статус платежа

GET /payments?orderId={id} - платежи по заказу

🔄 Статусы заказа
text
NEW → CONFIRMED → READY → ASSIGNED → DELIVERING → DELIVERED
↳ CANCELLED
💳 Статусы платежей
PENDING - создан, ожидает оплаты

SUCCESS - успешная оплата (заказ → CONFIRMED)

FAILED - ошибка оплаты (заказ → CANCELLED)

🧪 Тестирование
Запуск тестов
bash
mvn test
E2E сценарии
Файл food-delivery.http содержит полный сценарий:

Создание пользователя и ресторана

Добавление меню и позиций в корзину

Создание заказа и смена статусов

Платежи (успешные/неуспешные)

Негативные кейсы (409 Conflict)

Запуск в IntelliJ IDEA: откройте файл и нажмите "Run All"

🗂 Структура проекта
text
src/main/java/dev/marievski/fooddelivery/
├── cart/           # Корзина и позиции
├── common/         # Общие классы (исключения, enum)
├── order/          # Заказы и статусы
├── payment/        # Платежная система
├── restaurant/     # Рестораны и меню
├── user/           # Пользователи и версии
└── FoodDeliveryApplication.java
⚙️ Конфигурация
Основной файл конфигурации: src/main/resources/application.yml

H2 in-memory база данных

Автосоздание таблиц (ddl-auto: update)

Включен H2 Console для разработки

Форматирование SQL логов

## 🎯 Особенности реализации

- **DTO маппинг** через MapStruct
- **Валидация** на уровне контроллеров и сущностей
- **Единая обработка ошибок** через GlobalExceptionHandler
- **Версионирование пользователей** для истории изменений
- **Оптимизированные запросы к БД** - избегаем проблемы N+1 через EntityGraph
- **Оптимистичная блокировка** для предотвращения конфликтов

📝 Примечания
Проект использует H2 in-memory БД, не требует внешних зависимостей

Все бизнес-правила покрыты тестами и E2E сценариями

Готова документация API через Swagger UI